<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Основная дневная тема -->
    <style name="Theme.YandexMedia" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.YandexMedia.AppCompat" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/color_primary</item>
        <item name="colorPrimaryDark">@color/color_primary</item>
        <item name="colorAccent">@color/teal_200</item>

        <!-- Светлые цвета -->
        <item name="android:textColor">@color/color_black</item>
        <item name="android:windowBackground">@color/color_back_ground</item>
        <item name="android:navigationBarColor">@color/color_primary</item>
        <item name="android:statusBarColor">@color/color_primary</item>
    </style>

</resources>